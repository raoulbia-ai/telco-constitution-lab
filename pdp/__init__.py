"""Track 1 recoverability PDP package (Phase 1).

Provides the model-aware candidate-vs-running delta extraction (`state`) and the
commit-wrapping mediator (`mediator`) that enforces the deterministic
recoverability floor via OPA at the NETCONF transaction boundary.
"""
